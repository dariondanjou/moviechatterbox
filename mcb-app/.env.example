# Existing production Supabase instance (same values as the web app's
# NEXT_PUBLIC_SUPABASE_* in ../.env.local) — preserves live users (FR-8.1).
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
